dist: bionic
sudo: required

services: 
  - docker
before_install:
  - docker pull velocitymob/pandocker:buster
  

jobs:
  include:
  - stage: run docker
    script:
    - printenv
    - uname -a
    - lsb_release -a
    - ls -la
    - xelatex --version
    - pandoc --version
    - ls -l